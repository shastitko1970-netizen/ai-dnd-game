# Anthropic API Key
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: Proxy configuration for API requests (if behind corporate firewall)
# Format: http://user:password@host:port or https://user:password@host:port
PROXY_URL=
HTTP_PROXY=
HTTPS_PROXY=

# Server port
PORT=3001
NODE_ENV=development
